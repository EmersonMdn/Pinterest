<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/styles.css">

    <%- include("partials/head") %>

    <title><%= imagen.title%></title>
</head>
<%- include('partials/navbar') %>


<body>
    <div class="container p-4">
        <div class="row">
            <div class="col-md-6">
                <div class="card">
                    <img src="<%= imagen.path  %>" class="card-img-top image-detail">
                    <p class="card-text p-2"><%= format(imagen.created_at) %></p>
                    <div class="card-body">
                        <a href="/image/<%= imagen._id%>/delete" class="btn btn-danger btn-block">Delete image</a>
                    </div>
                </div>
            </div>

            <div class="col-md-6">
                <form method="post" action="/image/<%= imagen._id%>/update" enctype="multipart/form-data">
                    <div class="form-row">
                        <div class="form-group col-md-6">
                            <label for="id">ID</label>
                            <input type="text" class="form-control" value="<%= imagen._id %>" name="id">
                        </div>

                        <div class="form-group col-md-6">
                            <label for="title">Title</label>
                            <input type="text" class="form-control" value="<%= imagen.title %>" name="title">
                        </div>
                        <div class="form-group col-md-6">
                            <label for="title">Description</label>
                            <textarea class="form-control" name="description"><%= imagen.description %></textarea>
                        </div>
                        <div class="col-md-6">
                            <label for="">Size</label>
                            <input type="text" class="form-control" value="<%= imagen.size %>" name="size">
                        </div>
                        <div class="col-sm-6">
                            <label for="">Mimetype</label>
                            <input type="text" class="form-control" value="<%= imagen.mimetype %>" name="mimetype">
                        </div>
                        <div class="col-sm-6">
                            <label for="">Filename</label>
                            <input type="text" class="form-control" value="<%= imagen.filename %>" name="filename">
                        </div>
                        <div class="col-sm-6">
                            <label for="">Path</label>
                            <input type="text" class="form-control" value="<%= imagen.path %>" name="path">
                        </div>
                        <div class="col-sm-6">
                            <label for="">Originalname</label>
                            <input type="text" class="form-control" value="<%= imagen.orinalname %>" name="orinalname">
                        </div>
                        <div class="col-sm-6">
                            <label for="">Created at</label>
                            <input type="text" class="form-control" value="<%= imagen.created_at %>" name="created_at">
                        </div>

                        <button type="submit" class="btn btn-success btn-block m-2">Update</button>
                    </div>
            </div>
        </div>
    </div>
    </div>
    </div>
</body>

</html>